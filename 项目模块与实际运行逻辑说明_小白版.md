# 项目模块与实际运行逻辑说明（小白版）

更新日期：2026-09-10  
适用版本：分享包 v0.4.2

本次更新：两个顶层目录已按实际职责改名为 `招聘智能体` 和 `消息平台`；代码引用、快捷启动、测试、PRD、Skill 和 Windows 任务注册说明已同步。项目移动或改名后必须重新注册 Windows 任务，因为任务 action 保存绝对路径。

## 先说最容易弄反的两个文件夹

项目现在已经按实际职责命名：

| 文件夹 | 实际职责 | 不负责什么 |
|---|---|---|
| `招聘智能体/recruitment-agent` | 招聘智能体：BOSS/猎聘登录检查、岗位采集、筛选、评分、岗位库、日报、打招呼话术、招聘定时判断 | 不负责通用群聊路由 |
| `消息平台/message-platform` | 消息平台：飞书消息接收、发送、重试、幂等、审计、把群消息转给不同 Agent | 不决定招聘采集时间，不采集岗位，不做岗位评分 |

这两个名称就是当前有效路径。不要单独再改其中一个目录；如需移动整个分享包，代码相对路径仍可工作，但已经注册过的 Windows 任务必须在新位置重新注册。

## 一次完整流程发生什么

```text
Windows 独立时间触发器
  → 招聘调度器判断当前是否该运行
  → 查看 BOSS / 猎聘总开关
  → 对已开启平台做登录检查和岗位采集
  → 清洗字段、筛选、评分
  → 分平台写入长期岗位库并刷新状态
  → 生成当日日报草稿
  → 明确开启真实发送后，交给消息平台发飞书
  → 21:05 独立检查日报和发送回执
```

任一招聘平台关闭后，这个平台从登录到日报的整条链都会被排除，不会只关闭采集却继续拿旧数据做评分或日报。

## 定时逻辑在哪里改

唯一的人工时间配置文件是：

`招聘智能体/recruitment-agent/config/schedule_policy.json`

其他模块只读取这个文件：

- `src/core/schedule_policy.js`：检查时间格式、窗口重叠、成功间隔、不补跑和不唤醒等规则。
- `src/main/scheduled_entry.js`：根据当前时间与历史成功记录决定“采集、日报、采集后日报或跳过”。
- `bin/create_windows_task.example.ps1`：把 JSON 中每一个时间注册成独立 Windows 触发器。
- `bin/verify_windows_tasks.ps1`：检查 Windows 中的真实任务是否与 JSON 一致。

当前默认时间如下：

| 时段 | Windows 独立触发时间 | 实际限制 |
|---|---|---|
| 上午 | 09:00、10:00、11:00、12:00 | 上午最多成功采集一次 |
| 下午 | 13:00、14:00、15:00、16:00、17:00、18:00 | 下午最多成功采集一次；距上次成功至少 2 小时 |
| 日报 | 19:00、19:30、20:00、20:30 | 成功后不再发；失败满 30 分钟再试 |
| 日报缺失检查 | 21:05 | 独立检查日报、发送回执和任务配置 |

所有时间都遵守：

- 不唤醒电脑。
- 电脑没开或在睡眠时，错过就错过，不补跑。
- 每个时间点彼此独立，不依附于 09:00。
- `success` 才算“本半天已经成功”；`failed` 或 `partial_success` 不占成功次数，下一时间点可重试。

实际例子：09:00 电脑睡眠，09:00 不会执行；10:00 前电脑已恢复，10:00 是独立触发器，上午任务可以执行。旧方案的 10:00、10:30 等时间全依附于 09:00 起点，所以 09:00 错过会导致整天都不启动，现已取消该结构。

### 修改时间后的必要操作

1. 编辑 `config/schedule_policy.json`。
2. 先运行 `bin/create_windows_task.example.ps1 -GenerateOnly` 查看将要生成的时间，不改变电脑。
3. 得到操作者明确同意后，运行不带 `-GenerateOnly` 的注册脚本。
4. 注册脚本会自动运行 `bin/verify_windows_tasks.ps1`；必须显示一致才算完成。

只改 JSON、不重新注册 Windows 任务，操作系统仍按旧时间运行。

## BOSS 和猎聘整条任务流开关

配置文件：

`招聘智能体/recruitment-agent/config/platform_channels.json`

也可以在“启动控制台.cmd”打开的本地页面中勾选。

| 设置 | 含义 |
|---|---|
| `enabled=true` | 启用该平台的登录检查、采集、筛选、评分、岗位状态刷新和日报 |
| `enabled=false` | 关闭该平台整条链，连手工输入文件也不能绕过 |
| `detail_capture=true` | 列表读取后继续打开岗位详情，尽量补齐 JD |
| `detail_capture=false` | 只用列表摘要；不是退出登录，也不是关闭整个平台 |

分享包为了防止新用户未配置就访问招聘网站，BOSS 和猎聘总开关都默认关闭。BOSS 的详情开关默认开启；当用户开启 BOSS 总开关并完成登录后，会采集详情。

## BOSS 详情采集到底是什么意思

BOSS 列表页通常只提供岗位名、公司、薪资和少量摘要。不开详情时，程序不会逐个打开岗位卡片，因此可能缺少完整职责、任职要求等内容。这样的岗位可以作为“初步候选”，但不能假装信息完整，报告会提示人工确认。

详情模式现在会在处理**每一张**岗位卡片前重新恢复当前搜索词，并让详情链接在同一个受控浏览器标签中打开。这样不会出现“第一张能采，第二张开始回到默认推荐页”的问题，也不会因为链接另开窗口而不断遗留标签。如果详情接口没有返回目标岗位，程序会明确标记为“详情未采到”，不会再把包含多条岗位的整页文字误当成某一条岗位的 JD。

因此：

- 平台总开关决定“BOSS 整条链是否参加”。
- 详情开关决定“参加后是否打开岗位详情”。
- 登录状态决定“当前浏览器是否有权读取页面”。

三者不是同一个开关。首次登录说明和 AI Skill 已要求先向用户讲清楚这三件事。

## 为什么 BOSS 与猎聘采集仍然顺序执行

目前两个采集器共用一个项目专用 Chrome profile 和 CDP 连接。即使看起来可以开两个标签页，并行脚本仍可能同时切换页面、导航、抢占目标标签页或把另一个平台的页面误判成登录页。

因此当前做法是：

- 浏览器采集顺序执行，优先保证登录态和页面稳定。
- BOSS 与猎聘使用各自页面和各自采集器。
- 岗位库状态刷新已经按平台隔离；BOSS 刷新失败不会修改猎聘历史岗位，反之亦然。

如果以后要真正并行，应该给两个平台分配不同 Chrome profile、不同 CDP 端口和独立进程，而不只是多开标签页。

## 空薪资、空活跃度不是用户没填配置

这里有两类完全不同的数据：

- `targetSalaryK`：用户在画像规则中填写的目标薪资。首次配置检查会验证它，没填或填错会报错。
- `salary_min_k`、`salary_max_k`、`active_days`：招聘网站对某一条岗位返回的数据。网站可能没提供，空值是正常的“未知”。

旧问题是把网站的空值强制转成数字 `0`，可能错误理解成“月薪 0K”或“今天活跃”，从而错误加分、减分或过滤。现在空值保留为“未知”，不会冒充 0；这与用户是否填写目标薪资无关。

## “零岗位也成功”什么时候正常，什么时候是 Bug

正常场景：某个搜索词确实没有岗位，程序继续搜索其他词。即使所有关键词都正常返回 0 条，也应该生成一份写明“今天没有可推荐新岗位”的零结果日报。

异常场景：

- 没有开启任何处理阶段。
- 两个平台总开关都关闭，却要求运行招聘流程。
- 当天没有采集文件，却直接要求筛选、评分或日报。
- 应生成日报但没有任何报告产物。
- 发送模式是 `send`，却没有任何真实送达回执。

这些场景现在会失败并写日志，不再显示“运行成功、0 个输入、0 个输出”。

## `SCHEDULE_SEND_MODE` 是什么

它只决定定时日报是否真实发送：

- 没填写或写 `draft`：只生成本地草稿，不发飞书。
- 明确写 `send`：配置检查通过后才真实发送。

这是一条防误发边界。分享包默认 `draft`；正式项目如果需要自动日报，应明确配置为 `send`。21:05 watchdog 只有在正式发送模式下才要求查到真实送达回执。

## 飞书 HTTP 回调安全是什么意思

这不是“别人知道一个配置就能直接替你向飞书发消息”。风险方向相反：如果电脑把 HTTP 入站地址暴露出去，而服务端不鉴权，别人可能伪造一条“来自飞书群”的入站消息，诱导本地 Agent 执行路由任务。

现在 HTTP 回调默认要求：

- 服务只监听本机 `127.0.0.1`；除非维护者明确设置 `HTTP_HOST`，局域网其他电脑不能直接访问。
- Verification Token 正确。
- 配置 Encrypt Key 时，请求签名正确。
- 请求时间戳不能过旧。
- 相同 nonce/签名或 event_id 不能重复处理。

加密事件正文目前安全拒绝，并提示使用飞书官方 SDK 解密；本机长连接入口由飞书 SDK 在连接时认证，不经过这个 HTTP 回调。

## Node.js 最低版本

最低版本是 Node.js 22.4.0。原因不是“版本越新越好”，而是采集代码直接使用全局 `WebSocket`；Node 22.4.0 起该能力不再是实验状态。

以下入口都会检查版本：首次配置检查、环境检查、手工/定时招聘入口和 watchdog。低于 22.4.0 会先报错，不会运行一半才出现 `WebSocket is not defined`。

## 首次使用顺序

1. 运行“准备运行环境”，确认 Node.js 22.4.0+。
2. 从两个 `.env.example` 创建各自 `.env`，填写自己的配置；不要分享密钥。
3. 替换候选人画像，并填写目标城市、目标薪资和活跃度规则。
4. 在控制台只开启需要的 BOSS/猎聘整条任务流，确认 BOSS 详情采集保持开启。
5. 只为开启的平台运行可见登录脚本，人工完成登录或安全验证。
6. 先跑草稿测试，确认采集、评分和日报内容。
7. 查看 `schedule_policy.json`；确认后才注册 Windows 计划任务。
8. 最后才把 `SCHEDULE_SEND_MODE` 和 `WATCHDOG_SEND_MODE` 改为 `send`，并用测试群验证真实发送。

## 出问题时看哪里

| 现象 | 先看什么 |
|---|---|
| 某时间完全没启动 | Windows 计划任务、`bin/verify_windows_tasks.ps1`、系统睡眠记录 |
| 启动了但采集失败 | `logs/daily_workflow.log`、招聘网站登录状态、CDP 浏览器 |
| 没有日报 | `logs/scheduled_entry.log`、`data/schedule_state.json`、当天 outputs/drafts |
| 日报生成但没送达 | `data/push_state.json`、`logs/outbound_delivery.log`、消息平台发送审计 |
| 21:05 告警 | `logs/report_watchdog.log` 中的具体 reasons |
| 某平台历史岗位异常 | 查看状态刷新日志中的 `--platform=boss` 或 `--platform=liepin` |

watchdog 也是本机任务：如果电脑在 21:05 关机或睡眠，它同样不会唤醒电脑，也不会补跑。这符合当前产品约定。
